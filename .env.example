# Copy this file to `.env` and fill in real values. The real `.env` is git-ignored and must
# NEVER be committed. List every environment variable your project needs here, with a placeholder
# value and a one-line comment, so a new contributor knows what to set.

# EXAMPLE_API_KEY=your-key-here        # what it's for; where to get it
