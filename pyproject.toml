[build-system]
# >=77 for PEP 639: the SPDX `license` string and `license-files` below.
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "finale-file-parser"
version = "0.3.0"
description = "A parser for Finale music notation files (.mus/.musx)."
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "jsawruk" }]
keywords = ["finale", "music", "notation", "musicxml", "enigma", "musx"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Multimedia :: Sound/Audio :: Conversion",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "defusedxml>=0.7.1",
    # Engraves the score shown in the inspection report's Music tab. **LGPL-3.0**,
    # where this project is MIT: it is an ordinary install dependency, imported
    # and never vendored or statically linked, so the library's own licence is
    # unaffected -- but anyone redistributing a bundled application carries the
    # LGPL's obligations for it. See THIRD-PARTY-NOTICES.md and the 2026-08-15
    # entry in docs/DECISIONS.md.
    "verovio>=5",
]

[project.optional-dependencies]
# `finale-parser convert --format pdf`. Optional because most users want
# MusicXML and should not pay for a PDF stack they never call: without this
# extra the command exits with a message naming it, not an ImportError.
#
# Verovio cannot write PDF -- its toolkit renders SVG, MIDI, PAE and timemap
# and nothing else -- so a page has to go SVG -> PDF through a renderer.
# **svglib is LGPL-3.0**, the same obligation this project already carries for
# verovio and documents the same way; reportlab is BSD. Neither is vendored.
# The alternatives were measured rather than assumed: CairoSVG and WeasyPrint
# both need system libraries (cairo, and pango/harfbuzz/fontconfig
# respectively) that a wheel cannot supply, so `pip install` would stop being
# self-contained. See THIRD-PARTY-NOTICES.md and the 2026-08-20 entry in
# docs/DECISIONS.md.
pdf = [
    "svglib>=1.5",
    "reportlab>=4",
]

[project.urls]
Homepage = "https://github.com/jsawruk/finale-file-parser"
Repository = "https://github.com/jsawruk/finale-file-parser"
Issues = "https://github.com/jsawruk/finale-file-parser/issues"

[project.scripts]
# The command is shorter than the distribution, which is ordinary --
# python-dateutil gives you `dateutil`. See src/finale_file_parser/cli.py.
finale-parser = "finale_file_parser.cli:main"

[dependency-groups]
# Dev tooling. `uv sync` installs these by default.
dev = [
    "pytest>=7.4",
    "pytest-xdist>=3.5",
    "ruff>=0.4",
    "mypy>=1.8",
    "types-defusedxml>=0.7",
    # reportlab ships no py.typed, unlike svglib and verovio, and this codebase
    # is `mypy --strict`.
    "types-reportlab>=4",
    # verovio is an install dependency now, not a dev one -- see [project].
    # The `pdf` extra is installed for development so the gate exercises the
    # real renderer. The absent-extra path is covered by a test that hides the
    # import, which is the only way to check it while it is present here.
    "finale-file-parser[pdf]",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
# Ships the PEP 561 marker, without which an installing project's mypy ignores
# this package's annotations entirely -- the whole codebase is `mypy --strict`.
finale_file_parser = ["py.typed"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
# Start sensible; tighten as the project matures.
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unused_ignores = true
mypy_path = "src"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "scripts", "tests"]
